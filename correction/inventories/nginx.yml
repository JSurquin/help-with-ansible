---
all:
  vars:
    ansible_connection: docker
  children:
    nginx_servers:
      hosts:
        nginx1:
          ansible_host: nginx-server-1
          server_id: 1
        nginx2:
          ansible_host: nginx-server-2
          server_id: 2
