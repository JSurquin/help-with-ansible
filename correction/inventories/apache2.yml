---
all:
  vars:
    ansible_connection: docker
  children:
    apache_servers:
      hosts:
        apache1:
          ansible_host: apache-server-1
          server_id: 1
        apache2:
          ansible_host: apache-server-2
          server_id: 2
