---
# Single lab node to simplify reading the recap and handler audit log.
all:
  vars:
    ansible_connection: docker
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python3
  children:
    handler_lab:
      hosts:
        lab01:
          ansible_host: ansible-lab-web01
